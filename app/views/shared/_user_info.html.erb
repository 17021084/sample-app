<%= link_to gravatar_for(current_user, size: Settings.validations.user.avatar_size), current_user %>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to t("static_pages.home.profile"), current_user %>
</span>
<span>
  <%= t("microposts", count: current_user.microposts.count) %>
</span>
